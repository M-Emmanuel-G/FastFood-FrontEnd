import { ContainerFooter } from "./style"

export const Footer = ()=>{
    return(
        <ContainerFooter>
            <span>Desenvolvido por Márcio Emmanuel Gonçalves</span>
        </ContainerFooter>
    )
}